{
  "name": "shadowguard-extension",
  "version": "0.1.1",
  "private": true,
  "description": "ShadowGuard browser extension — detects and redacts sensitive data before it reaches AI chat tools.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit -p tsconfig.json && vite build",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.28",
    "@types/chrome": "^0.0.270",
    "typescript": "^5.5.4",
    "vite": "^5.4.2"
  }
}
